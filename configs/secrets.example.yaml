# secrets.yaml — add this file to .gitignore!
# This file holds real credentials. GhostKey maps ghost tokens to these values
# at the network layer. The AI agent never sees these values.
#
# Format: ghost_token: real_token
mappings:
  # "GHOST::openai-prod": "sk-proj-actualrealtoken..."
  # "GHOST::github-ci": "ghp_actualrealtoken..."
  # "GHOST::aws-dev": "AKIAIOSFODNN7EXAMPLE..."
